import React, { Component } from 'react'
import { withRouter } from 'react-router-dom'
import Container from '../../Container'

class UpdateDemand extends Component {
  render() {
    return <Container />
  }
}

export default withRouter(UpdateDemand)
